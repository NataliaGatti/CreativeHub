<!--
  Todo esto es lo que se manda y se recibe del otro lado, habraia
  que acomodarlo despues para que sea con el diseño hecho por Nati 
-->
<div class="message-container" id="message-<%= message.id %>">
  <i class="author">
    <span><%= message.user.name %></span>
    <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
  </i>
  <p><%= message.content %></p>
</div>